# NativeScript ImageCacheIt Android

## Build

Go to folder `imagecacheit`. Run gradle tasks: `clean`, `assembleRelease`.

Use the aar package: `imagecacheit/build/outputs/aar/imagecacheit-release.aar`.
